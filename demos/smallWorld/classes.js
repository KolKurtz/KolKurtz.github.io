//CLASS DEFS

//A grid tile
//Owns its reference(cartesian), mineral count

class GridTile
{
  constructor(reference)
  {
    this.reference.x = tileX;
    this.reference.y = tileY;
	  this.mineralCont = 0;
  }
}
